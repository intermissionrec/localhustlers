jinja2>=3.1
streamlit>=1.32
# Needs Python 3.9+. On an older interpreter this line is skipped entirely
# (not an error) — the editor falls back to plain ↑/↓ reorder buttons.
streamlit-sortables>=0.3.1; python_version >= "3.9"